const initialState = {
  historyOperations: [],
};

const operationReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default operationReducer;
